import React from "react";

import { Text, View } from "react-native";
import {Tabs} from "expo-router";

const Cart = () => {
  return (
    <View>
        <Tabs />
    </View>
  );
};

export default Cart;
